extends: default

rules:
  line-length:
    max: 200
  document-start: disable
  indentation:
    spaces: 2
  comments:
    min-spaces-from-content: 1
  truthy:
    allowed-values: ['true', 'false']
    check-keys: false